# Core runtime dependencies for the stable ComputerVision Counter Images .pt workflow.
# start.bat creates a .venv and installs these dependencies.
# Ultralytics resolves its own ML stack dependencies, including torch/torchvision.
# ONNX, ONNX Runtime and segmentation-specific dependencies are intentionally
# not included for the v0.2.0 release candidate.

numpy>=1.26.4
opencv-python>=4.8.0
Pillow>=10.0.0
ultralytics>=8.3.0

# Cleanup package notes

This package is a safe overlay for the current `ComputerVisionCounter_Images` repository.

It does not include copied Python source files from GitHub. That is intentional: the task is to clean repository hygiene and documentation without changing working code. Keep the current `.py` files from the repository and apply these files around them.

The future `src/` migration is documented as a known challenge, not performed here.

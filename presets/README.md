# presets

This folder can contain small tracked preset examples.

Local/private preset variants should use names such as `*.local.json` or be placed in `presets/local/`; those files are ignored by Git.

# models

Place local YOLO model files here during testing, for example `.pt` weights.

Model files are not included in the repository and should not be committed unless there is a clear license decision.
